{
  "version": 1,
  "sources": [
    {
      "id": "case_records",
      "driver": "sqlite",
      "schema_notes": "cases(case_code TEXT, disclosure_approved INTEGER 0 or 1) contains synthetic case approval evidence from case-schema.sql.",
      "timeout_seconds": 5,
      "max_rows": 10,
      "queries": [
        {
          "name": "verify_case_approval",
          "title": {
            "en": "Case disclosure approval",
            "cs": "Schválení zpřístupnění případu"
          },
          "description": "Look up the case code in the approved evidence view and verify whether disclosure is approved.",
          "sql": "SELECT case_code, disclosure_approved FROM cases WHERE case_code = :case_code",
          "parameters": {
            "case_code": {
              "type": "string",
              "description": "Case code from the message"
            }
          },
          "required": [
            "case_code"
          ],
          "mode": "conditional",
          "when": "The message requests access to or disclosure of information about a case, in any language.",
          "require_rows": true
        }
      ],
      "path": "case-records.sqlite3",
      "tables": [
        "cases"
      ]
    }
  ]
}
